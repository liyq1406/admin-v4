import { formatDate, uniformDate, uniformTime, uniformMinuteTime } from './format-date'
import bytesPrettify from './bytes-prettify'
import toFixed from './to-fixed'
import toPercentage from './to-percentage'

export default {
  formatDate,
  uniformDate,
  uniformTime,
  uniformMinuteTime,
  bytesPrettify,
  toFixed,
  toPercentage
}
